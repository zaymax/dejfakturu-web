import { EmptyState } from "@/components/app-shell/app-shell";

export default function ExpensesPage() {
  return (
    <EmptyState
      title="Výdaje připravujeme"
      description="Tahle sekce je v prototypu jako budoucí rozšíření. V MVP zatím zůstává jako placeholder."
    />
  );
}
